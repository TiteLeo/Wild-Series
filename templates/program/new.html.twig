{% extends "base.html.twig" %}

{% block body %}
    <h1>Add a new program : </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Add Program</button>
    {{ form_end(form) }}

{% endblock %}